# ===========================================
# 必要环境变量（缺失将导致应用无法访问）
# ===========================================

# GitHub OAuth 配置
# 在 GitHub → Settings → Developer settings → OAuth Apps 获取
GITHUB_CLIENT_ID=your_client_id
GITHUB_CLIENT_SECRET=your_client_secret

# JWT 密钥（用于用户认证会话签名）
# 生成命令: openssl rand -hex 32
JWT_SECRET=your_jwt_secret

# 加密密钥（用于加密存储私钥和 Webhook Secret）
# 生成命令: openssl rand -hex 32
ENCRYPTION_KEY=your_encryption_key

# 数据库连接字符串
# 格式: postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://user:password@localhost:5432/manticore

# ===========================================
# 可选环境变量（CI/CD 功能需要）
# ===========================================

# GitHub App 配置
# 在 GitHub → Settings → Developer settings → GitHub Apps 获取
GITHUB_APP_ID=123456
GITHUB_PRIVATE_KEY_PATH=./private-key.pem

# Webhook Secret（网页端不支持自动生成，需自行设定并填入 GitHub App 设置）
# 生成命令: openssl rand -base64 32
GITHUB_WEBHOOK_SECRET=your_webhook_secret

# 仓库配置
REPO_OWNER=your_username
REPO_NAME=your_repo

# Webhook 签名验证密钥（与 GITHUB_WEBHOOK_SECRET 保持一致）
WEBHOOK_SECRET=your_webhook_secret

# 服务器配置
PORT=3001

# 合作者列表（逗号分隔，用于手动重试鉴权）
COLLABORATORS=user1,user2
